mutation UpdateUser($userId: Float!, $nickName: String, $description: String) {
  updateUser(userId: $userId, data: { nickName: $nickName, description: $description }) {
    description
    image
    hashedPassword
    xp
    friends {
      id
    }
  }
}
